# ShipItAI Self-Hosted Configuration
# Copy this file to .env and fill in your values

# GitHub App Configuration (required)
# Create a GitHub App at https://github.com/settings/apps
GITHUB_APP_ID=123456
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# GitHub App Private Key (required)
# Generate this in your GitHub App settings and paste the entire PEM content
# Include the -----BEGIN RSA PRIVATE KEY----- and -----END RSA PRIVATE KEY----- lines
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
Your private key content here
-----END RSA PRIVATE KEY-----"

# Anthropic API Key (required)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Bot name for @mentions (optional, defaults to "shipitai")
BOT_NAME=shipitai

# PostgreSQL credentials (used by docker-compose)
# Change these before deploying to production!
POSTGRES_USER=shipitai
POSTGRES_PASSWORD=changeme
POSTGRES_DB=shipitai

# Database URL (optional when using docker-compose, it's auto-configured)
# Only needed if you want to use an external PostgreSQL instance
# DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=disable
